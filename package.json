{
  "name": "text-particles-animation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "npm run typecheck",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "@angular/core": "^21.2.10",
    "rxjs": "^7.8.2",
    "zone.js": "^0.16.1"
  }
}
